  <div class="banner text-center">
    <h2><span data-controller="typed"></span></h2>
    <%= link_to "Browse all books",  books_path, class:'btn' %>
  </div>

  <div class="header">
    <h2> Books of the Week</h2>
  </div>
  <div class="cards-wrapper">
      <% @books.each do |book| %>
        <%= render 'books/book_card', book: book %>
      <% end %>
  </div>
